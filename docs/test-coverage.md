# Behaviour test coverage migration map

This is the immutable migration inventory for the behaviour matrix at commit
`786b67884cef8eed3c1690a756c92ba9fef29a04`. It records all **255** original table rows
before consolidation. Old rows are identified by section and ID; the two distinct
`Note editing / E42` rows are intentionally retained as two inventory entries and receive
different canonical IDs.

The original identifier collisions are: `S*` (Playback speed and Separation panel),
`N*` (Notes lane and Language), `T*` (Transport and Tempo grid), plus the duplicate
`Note editing / E42`. Canonical IDs are globally unique. A row remains retained until its
disposition and evidence are deliberately updated; no related helper test is counted as
coverage unless it proves the stated contract.

Contract groups correspond to the product-contract sections: loading music; playback and
routing; transport, loops, and speed; local stem separation; notes, tempo, and visual
reference; editing and persistence; human-readable export; language and resilience; and
release integrity.

| Section | Old ID | Canonical ID | Short contract | Disposition | Automated test | Retained live check |
|---|---|---|---|---|---|---|
| Loading | `L1` | `LOAD-001` | One audio file loads as a single lane labelled **Full mix**. | retain | — | `Loading / L1` |
| Loading | `L2` | `LOAD-002` | Stems named `vocals`/`guitar`/`bass`/`drums`/`piano`/`other` land in those lanes in that order, whatever order they sit in inside the zip. | retain | — | `Loading / L2` |
| Loading | `L3` | `LOAD-003` | An unrecognised filename still gets its own lane, labelled with the filename. | retain | — | `Loading / L3` |
| Loading | `L4` | `LOAD-004` | A file whose name matches `mix`/`full`/`master`/`original` **and** which sits alongside stems becomes the Full mix lane, and is muted whenever anything else is unmuted. | retain | — | `Loading / L4` |
| Loading | `L5` | `LOAD-005` | An undecodable file is skipped with a message naming it; the rest still load. | retain | — | `Loading / L5` |
| Loading | `L6` | `LOAD-006` | ⚠ Dropping a **folder** is unsupported on every protocol and must not be "fixed" — it says so and tells the user to zip it. | retain | — | `Loading / L6` |
| Loading | `L7` | `LOAD-007` | The `AudioContext` is 44100 Hz regardless of the machine's default. | retain | — | `Loading / L7` |
| Loading | `L8` | `LOAD-008` | A `.zip` of stems loads to exactly the lanes the same folder would, and the song title comes from the folder name **inside** the zip. | retain | — | `Loading / L8` |
| Loading | `L8a` | `LOAD-009` | A **flat** zip — stems at the root, no enclosing folder — is titled from the zip's own filename, minus `.zip`. | retain | — | `Loading / L8a` |
| Loading | `L9` | `LOAD-010` | Both stored and deflated zips load — the app's own Save stems output and anything from Finder "Compress" or `zip -r`. | retain | — | `Loading / L9` |
| Loading | `L10` | `LOAD-011` | Finder's `__MACOSX/._*` sidecars are ignored, so a Finder-made six-stem zip gives six lanes, not twelve. | retain | — | `Loading / L10` |
| Loading | `L11` | `LOAD-012` | A zip that is unreadable says why: not a zip, a damaged directory, Zip64, encrypted, unsupported compression, truncated, corrupt, or no audio inside. | retain | — | `Loading / L11` |
| Loading | `L12` | `LOAD-013` | A drop with nothing usable in it names which case it was: a folder, more than one file, or neither a song nor a zip. None is silent. | retain | — | `Loading / L12` |
| Loading | `L13` | `LOAD-014` | Exactly two things load, by button or by drop: **one** audio file (a whole song), or **one** `.zip` of stems. | retain | — | `Loading / L13` |
| Loading | `L18` | `LOAD-015` | There is **one** load button, not two. It accepts either kind and picks the behaviour from the extension: `.zip` → `loadZip`, anything else → `loadSong`. | retain | — | `Loading / L18` |
| Loading | `L19` | `LOAD-016` | Picking the **same** file twice in a row loads it twice. | retain | — | `Loading / L19` |
| Loading | `L14` | `LOAD-017` | A set of loose stem files is **not** a supported input — the user is told to zip them. Six files dropped together give a message, not six lanes. | retain | — | `Loading / L14` |
| Loading | `L15` | `LOAD-018` | Dragging a file anywhere over the window shows `#drag-overlay`, and the window accepts the drop. A dropped file must **never** make the browser navigate to it. | retain | — | `Loading / L15` |
| Loading | `L16` | `LOAD-019` | The overlay is the drop target in **both** states, before a song loads and after. Dropping a second song over a loaded player is the common case and `#dropzone` is hidden by then. | retain | — | `Loading / L16` |
| Loading | `L17` | `LOAD-020` | The overlay does not flicker while the cursor crosses lanes and panels. | retain | — | `Loading / L17` |
| Lanes and muting | `M1` | `MIX-001` | Clicking a lane's name block toggles **only** that lane. Others are untouched. | automate | `tests/routing-state.test.js#toggles only the requested lane and enters custom mode` | `Lanes and muting / M1` |
| Lanes and muting | `M2` | `MIX-002` | The click target is the whole left column — full lane height, flush to the lane's left edge, through the number badge. | retain | — | `Lanes and muting / M2` |
| Lanes and muting | `M2a` | `MIX-003` | That target is visibly a target **without hovering**: its own background tint, distinct from the lane, and a divider against the waveform. | retain | — | `Lanes and muting / M2a` |
| Lanes and muting | `M2b` | `MIX-004` | The player says in words that the name block toggles the track. | retain | — | `Lanes and muting / M2b` |
| Lanes and muting | `M3` | `MIX-005` | Clicking the waveform seeks and does **not** toggle. | retain | — | `Lanes and muting / M3` |
| Lanes and muting | `M4` | `MIX-006` | Keys `1`–`6` toggle the same lanes as clicking their names. | retain | — | `Lanes and muting / M4` |
| Lanes and muting | `M5` | `MIX-007` | A muted lane is dimmed, and its gain is 0. | retain | — | `Lanes and muting / M5` |
| Lanes and muting | `M6` | `MIX-008` | The per-lane volume slider is independent of mute. | retain | — | `Lanes and muting / M6` |
| Unmute all / restore | `U1` | `MIX-009` | With anything muted, pressing turns **every** lane on. | automate | `tests/routing-state.test.js#snapshots the current partial mix and restores it exactly` | `Unmute all / restore / U1` |
| Unmute all / restore | `U2` | `MIX-010` | Pressing again returns to exactly the lanes that were on before. | automate | `tests/routing-state.test.js#snapshots the current partial mix and restores it exactly` | `Unmute all / restore / U2` |
| Unmute all / restore | `U3` | `MIX-011` | The snapshot is taken **when everything is turned on**, not when a lane is muted. So: all on → mute one lane → press → all on → press → back to *that one lane muted*, not to an older state. | automate | `tests/routing-state.test.js#snapshots the current partial mix and restores it exactly` | `Unmute all / restore / U3` |
| Unmute all / restore | `U4` | `MIX-012` | Label follows state: **Unmute all** when anything is muted, **Restore previous** when everything is on and a snapshot exists. | automate | `tests/routing-state.test.js#labels partial, restorable, and fresh all-on states` | `Unmute all / restore / U4` |
| Unmute all / restore | `U5` | `MIX-013` | Everything on with **nothing saved** — the state a freshly loaded or freshly separated song starts in — the press **mutes every lane**, and the label reads **Mute all**. Pressing again brings them all back. The button is never disabled. | automate | `tests/routing-state.test.js#mutes all on the first all-toggle and returns to all-on without saving silence` | `Unmute all / restore / U5` |
| Unmute all / restore | `U5a` | `MIX-014` | All lanes off does **not** take a snapshot — "restore previous" meaning "silence again" is not offered. After mute-all → unmute-all the label is **Mute all** again, not **Restore previous**. | automate | `tests/routing-state.test.js#mutes all on the first all-toggle and returns to all-on without saving silence` | `Unmute all / restore / U5a` |
| Unmute all / restore | `U6` | `MIX-015` | A new song clears the snapshot. | automate | `tests/routing-state.test.js#resets mode, mutes, and stale snapshots on song load` | `Unmute all / restore / U6` |
| Unmute all / restore | `U7` | `MIX-016` | With a full-mix file present, "unmute all" unmutes the stems and drives the mix lane to 0 — never both. | automate | `tests/routing-state.test.js#unmutes stems while suppressing an explicit mix` | `Unmute all / restore / U7` |
| Unmute all / restore | `U8` | `MIX-017` | The button is styled identically to **Save stems (.zip)** (`btn ghost`). | retain | — | `Unmute all / restore / U8` |
| Play dropdown | `P1` | `MIX-018` | Picking an instrument solos it — every other lane mutes. | automate | `tests/routing-state.test.js#solos %s from the play dropdown` | `Play dropdown / P1` |
| Play dropdown | `P2` | `MIX-019` | **Full mix** turns every lane on, or plays the mix file if one exists. | automate | `tests/routing-state.test.js#treats every ordinary lane on as Full mix` | `Play dropdown / P2` |
| Play dropdown | `P3` | `MIX-020` | Any per-lane change switches the dropdown to **Custom…**. | automate | `tests/routing-state.test.js#toggles only the requested lane and enters custom mode` | `Play dropdown / P3` |
| Play dropdown | `P5` | `MIX-021` | After choosing from the dropdown, the keyboard shortcuts still work. The select does not keep focus. | retain | — | `Play dropdown / P5` |
| Play dropdown | `P4` | `MIX-022` | With no mix file, "all lanes on" shows **Full mix**, not Custom — all six on *is* the full mix. | automate | `tests/routing-state.test.js#treats every ordinary lane on as Full mix` | `Play dropdown / P4` |
| Transport | `T1` | `TRN-001` | All lanes stay sample-locked — one clock, one `t0`. | retain | — | `Transport / T1` |
| Transport | `T2` | `TRN-002` | Muting never stops a source; it ramps gain. The track stays on the timeline. | retain | — | `Transport / T2` |
| Transport | `T3` | `TRN-003` | ⚠ End of song is detected on the audio graph (`onended` on the longest source), not in `requestAnimationFrame`. | retain | — | `Transport / T3` |
| Transport | `T4` | `TRN-004` | ⚠ Seeking while playing resumes playing; seeking while stopped stays stopped. | retain | — | `Transport / T4` |
| Transport | `T5` | `TRN-005` | `←`/`→` nudge 5 s. | retain | — | `Transport / T5` |
| A–B repeat | `R1` | `LOOP-001` | ⚠ `a` and `b` set points at the playhead; the region repeats until cleared. | retain | — | `A–B repeat / R1` |
| A–B repeat | `R2` | `LOOP-002` | ⚠ Points set in either order work — a B before an A swaps itself. | retain | — | `A–B repeat / R2` |
| A–B repeat | `R3` | `LOOP-003` | ⚠ Points closer than 0.1 s are rejected with a message, and the second point is discarded. | retain | — | `A–B repeat / R3` |
| A–B repeat | `R4` | `LOOP-004` | ⚠ Looping runs on the audio thread (`loop`/`loopStart`/`loopEnd`), so it survives a backgrounded tab. | retain | — | `A–B repeat / R4` |
| A–B repeat | `R5` | `LOOP-005` | ⚠ A stem shorter than `loopEnd` is left unlooped and falls silent rather than wrapping early and drifting. | retain | — | `A–B repeat / R5` |
| A–B repeat | `R6` | `LOOP-006` | The badge is hidden when no point is set. | retain | — | `A–B repeat / R6` |
| A–B repeat | `R7` | `LOOP-007` | ⚠ Loading a song clears both points. | retain | — | `A–B repeat / R7` |
| A–B repeat | `R8` | `LOOP-008` | A note **still sounding at A** plays its remainder rather than being skipped, on every lap. | retain | — | `A–B repeat / R8` |
| A–B repeat | `R9` | `LOOP-009` | It **resumes** the envelope rather than re-attacking — quieter at A than the same note played from its start. | retain | — | `A–B repeat / R9` |
| A–B repeat | `R10` | `LOOP-010` | A note is **cut at B**, not left ringing across the restart. | retain | — | `A–B repeat / R10` |
| A–B repeat | `R11` | `LOOP-011` | **Seeking** into a note does the same thing as entering at A. | retain | — | `A–B repeat / R11` |
| A–B repeat | `R12` | `LOOP-012` | A remainder under **10 ms** is dropped, and no whole note ever is. | retain | — | `A–B repeat / R12` |
| A–B repeat | `R13` | `LOOP-013` | An untrusted note straddling A stays **silent**, as N36 requires. | retain | — | `A–B repeat / R13` |
| Playback speed | `S1` | `SPD-001` | A speed control (slider, 10–150%, step 5) is present in the controls bar, alongside Volume. | retain | — | `Playback speed / S1` |
| Playback speed | `S2` | `SPD-002` | Always starts at 100% when a song loads — never persisted across songs or reloads. | retain | — | `Playback speed / S2` |
| Playback speed | `S3` | `SPD-003` | ⚠ Changing speed away from 100% audibly changes tempo while the pitch stays the same. | retain | — | `Playback speed / S3` |
| Playback speed | `S4` | `SPD-004` | At exactly 100% the native, unprocessed playback path runs — zero behaviour change from before this feature. | retain | — | `Playback speed / S4` |
| Playback speed | `S5` | `SPD-005` | ⚠ Crossing the 100% ↔ non-100% boundary rebuilds the audio graph (same `stop()`→`play()` pattern as a loop-bounds change); staying on one side of it while dragging the slider does **not** restart the audio. | retain | — | `Playback speed / S5` |
| Playback speed | `S6` | `SPD-006` | `[` / `]` nudge the rate ±5%, clamped to [10, 150]; Shift+`[` / Shift+`]` nudge ±1% over the same range; `\` resets to 100%. | retain | — | `Playback speed / S6` |
| Playback speed | `S7` | `SPD-007` | A–B looping and seeking still work at non-100% rates. | retain | — | `Playback speed / S7` |
| Playback speed | `S8` | `SPD-008` | Sonify reference tones (Notes lane) stay locked to the (possibly slowed/sped) stems. | retain | — | `Playback speed / S8` |
| Playback speed | `S9` | `SPD-009` | A time-stretched A–B loop can have a faint discontinuity at the seam — accepted, not fixed by this feature. Native 100% looping is unaffected. | retain | — | `Playback speed / S9` |
| Playback speed | `S10` | `SPD-010` | Returning to 100% falls back to native playback with no lingering artifacts. | retain | — | `Playback speed / S10` |
| Playback speed | `S11` | `SPD-011` | The current speed is shown next to every time-code — the main transport, and the Overview/Zoom lanes when present — always visible, not just away from 100%. | retain | — | `Playback speed / S11` |
| Playback speed | `S12` | `SPD-012` | When a drums stem has a confidently detected tempo (see Tempo grid, T-series below), a calculated/original BPM readout appears next to the speed tag — the BPM a metronome would need at the CURRENT rate, over the BPM notes.js reports (auto-detected or manually overridden). Absent until then. | retain | — | `Playback speed / S12` |
| Separation panel | `S1` | `SEP-001` | The panel appears only for a single unseparated track. A stems folder loaded from disk shows no panel. On a handheld the same rule holds, but the panel's *contents* are the explanation — see S14. | retain | — | `Separation panel / S1` |
| Separation panel | `S2` | `SEP-002` | While a run is in flight: **Separate** disabled, **Cancel** shown, **Save** disabled. | retain | — | `Separation panel / S2` |
| Separation panel | `S3` | `SEP-003` | Save is disabled mid-run specifically so a *previous* track's stems can't be written, and so encoding doesn't compete with the worker for memory. | retain | — | `Separation panel / S3` |
| Separation panel | `S4` | `SEP-004` | On success the status line goes **empty** — no "done". Six lanes replacing one is the confirmation. | retain | — | `Separation panel / S4` |
| Separation panel | `S5` | `SEP-005` | On success **Separate** disappears; **Save stems (.zip)** appears. | retain | — | `Separation panel / S5` |
| Separation panel | `S6` | `SEP-006` | Loading a fresh single track brings **Separate** back and hides **Save**. | retain | — | `Separation panel / S6` |
| Separation panel | `S7` | `SEP-007` | The separated result has **six** lanes. The original mix track is dropped, not kept and suppressed. | retain | — | `Separation panel / S7` |
| Separation panel | `S8` | `SEP-008` | Every lane starts unmuted after separation. | retain | — | `Separation panel / S8` |
| Separation panel | `S9` | `SEP-009` | If the mix is playing when the stems land, playback stops and the playhead returns to 0. | retain | — | `Separation panel / S9` |
| Separation panel | `S10` | `SEP-010` | Cancelling leaves the panel usable and reports "cancelled". | retain | — | `Separation panel / S10` |
| Separation panel | `S11` | `SEP-011` | A worker that dies without posting (OOM) still releases the UI. | retain | — | `Separation panel / S11` |
| Separation panel | `S12` | `SEP-012` | ⚠ Model download progress is reported in MB, then the backend (`webgpu` or CPU) is named. | retain | — | `Separation panel / S12` |
| Separation panel | `S13` | `SEP-013` | There is no local-`.onnx` picker. The worker still accepts a `modelBuffer`, but nothing in the UI supplies one. | retain | — | `Separation panel / S13` |
| Separation panel | `S14` | `SEP-014` | On a handheld, a loaded single song shows `#sep` containing only the explanation. **Separate**, **Save**, **Cancel** and the progress bar are all gone. | retain | — | `Separation panel / S14` |
| Separation panel | `S15` | `SEP-015` | On a handheld the drop zone makes no in-browser-separation promise. | retain | — | `Separation panel / S15` |
| Separation panel | `S16` | `SEP-016` | Both handheld strings follow the language toggle like every other string. | retain | — | `Separation panel / S16` |
| Notes lane | `N1` | `NOTE-001` | Each panel appears only once that channel actually **has notes** — not merely once its stem is loaded, same principle as the meta/tune rows inside it (N22b) and `#notes-tempo` (T1). `#notes-vocals` needs vocals notes, `#notes-bass` needs bass notes, independently of each other. A song with only one melodic stem never shows the other panel at all, before or after Find Notes; a stem that IS loaded still shows nothing — label, Export/Import, Export list, all of it — until its own detection completes. | retain | — | `Notes lane / N1` |
| Notes lane | `N2` | `NOTE-002` | Detection never starts by itself, for either channel. It costs ~7 s of CPU on a cold first run, which is a surprise rather than a convenience. | retain | — | `Notes lane / N2` |
| Notes lane | `N3` | `NOTE-003` | While analysing: the shared **Find notes** button (`#notes-go-all`) disabled, a spinner, and a "Detecting: …" hint next to it naming exactly which stem(s) are still in flight — not a generic message, and not the old shared `#status` line, so one channel finishing first is never mistaken for the whole run being done. No progress bar: `f0Track` reports no progress, and a bar that jumps 0 → 100 is worse than none. | retain | — | `Notes lane / N3` |
| Notes lane | `N4` | `NOTE-004` | On success the status line goes **empty** — the lane appearing is the confirmation. | retain | — | `Notes lane / N4` |
| Notes lane | `N5` | `NOTE-005` | Each stem's own notes lane sits directly under that stem's own waveform lane — a bass-notes lane under the bass waveform, a vocals-notes lane under the vocals waveform. The one shared **overview** lane and, directly below it, the one shared **zoomed pane** dock above whichever comes first, vocals-priority: DOM order at the top of `#lanes` is `[.lane.overview, .lane.ribbon-zoom, vocals waveform, vocals .ribbon]`, with the bass `.ribbon` lane elsewhere in `#lanes`, directly under the bass waveform lane. | retain | — | `Notes lane / N5` |
| Notes lane | `N6` | `NOTE-006` | The ribbon is on the same time grid as every waveform: its canvas is the same CSS width, and it reflows with them on resize. | retain | — | `Notes lane / N6` |
| Notes lane | `N7` | `NOTE-007` | Clicking the ribbon seeks, like any other lane. | retain | — | `Notes lane / N7` |
| Notes lane | `N8` | `NOTE-008` | Moving **Shortest note** changes the note count **without re-running analysis**. | retain | — | `Notes lane / N8` |
| Notes lane | `N9` | `NOTE-009` | **Fit the lane to the melody** is **off by default** — ticking it narrows the lane's vertical range to the melody, rather than the reverse. | retain | — | `Notes lane / N9` |
| Notes lane | `N10` | `NOTE-010` | A note outside the clipped range is drawn at the lane edge in the A–B orange, never dropped. A hidden note would be a silent lie. | retain | — | `Notes lane / N10` |
| Notes lane | `N10a` | `NOTE-011` | Clip is **display only**. It feeds `pitchRange()` and nothing else: it is not passed to `interpret()`, so the note list is identical either way, and a clipped note still *sounds* at its detected pitch. | retain | — | `Notes lane / N10a` |
| Notes lane | `N11` | `NOTE-012` | The contour breaks at every unvoiced run and never bridges one, in both panes. | retain | — | `Notes lane / N11` |
| Notes lane | `N12` | `NOTE-013` | Loading a new song clears both lanes, even when the new song has the same stems. The old frames describe the old audio. | retain | — | `Notes lane / N12` |
| Notes lane | `N13` | `NOTE-014` | Neither lane is in `tracks`. Each has its own mute and volume but **no number key**, and mute-all, solo and the stem count all ignore both. | retain | — | `Notes lane / N13` |
| Notes lane | `N14` | `NOTE-015` | The lane label follows the language toggle. The note **names** drawn inside it never translate, exactly as stem ids and filenames do not. | retain | — | `Notes lane / N14` |
| Notes lane | `N15` | `NOTE-016` | Each lane plays its own notes as tones, **muted by default**, independently of the other. Clicking a lane's name toggles only that lane. | retain | — | `Notes lane / N15` |
| Notes lane | `N16` | `NOTE-017` | The synth is locked to the same `t0` as the stems, and follows A–B repeat by generating laps. | retain | — | `Notes lane / N16` |
| Notes lane | `N17` | `NOTE-018` | Dragging the lane's bottom grip resizes it, and the height survives a reload. | retain | — | `Notes lane / N17` |
| Notes lane | `N18` | `NOTE-019` | One shared zoomed pane sits directly above whichever note lane comes first (vocals-priority), showing a window of the song rather than all of it. Unlike the per-stem note lanes, it is **always visible** — from the moment a vocals or bass stem loads, before Find Notes has ever run — since it's useful as a plain-waveform tool on its own; only the Notes chips and Edit toggle *inside* it wait for detection (N63). It follows the playhead while playing, panning by dragging when stopped. | retain | — | `Notes lane / N18` |
| Notes lane | `N19` | `NOTE-020` | The wheel zooms the pane about the cursor, between 2 s and 60 s, and the width survives a reload. | retain | — | `Notes lane / N19` |
| Notes lane | `N20` | `NOTE-021` | At whole-song width the lane draws the contour as a per-pixel band, not a polyline. A polyline there joins pitches ~26 frames apart and buries the notes under vertical strokes. | retain | — | `Notes lane / N20` |
| Notes lane | `N21` | `NOTE-022` | Note names are labelled on every semitone at ≥7 px per semitone, fall back to C only between 6 and 7 px, and disappear entirely below 6 px. | retain | — | `Notes lane / N21` |
| Notes lane | `N22` | `NOTE-023` | One shared **Find notes** button (`#notes-go-all`), not one per panel: clicking it runs analysis on every channel that still needs it (has a stem, no notes yet) and leaves the rest alone. Once notes are found in a panel, that panel's own show/hide toggle appears — the same swap the separation panel does with Separate → Save. Once **every** melodic stem present has been analysed, the whole `#notes-detect` section hides outright (N22a) rather than sitting there disabled — nothing it could still do for this song. Loading a new song brings the section and both toggles back. | retain | — | `Notes lane / N22` |
| Notes lane | `N22a` | `NOTE-024` | `#notes-detect` has three states, not two: **disabled** (visible) when no melodic stem was ever loaded — nothing this button could ever do for this song, so it stays legible rather than vanishing; **enabled** when at least one present stem still needs analysis; **hidden** once every present stem has been analysed. A zip with only a bass stem (no vocals) leaves it enabled and, clicked, analyses bass only — `#notes-vocals` stays hidden throughout, per its own has-notes gate (N1), and is never touched; once that bass run lands, the section hides (there is no vocals left to wait for). A zip with **neither** melodic stem disables it immediately on load, with no click needed to observe it, and it never hides since there is nothing to ever finish. | retain | — | `Notes lane / N22a` |
| Notes lane | `N22b` | `NOTE-025` | Each panel's count/toggle/簡譜/key row (`#notes-meta-vocals` / `-bass`) is hidden until that channel actually has notes, same as its advanced tune row (`#notes-tune-vocals` / `-bass`) already was — an empty count and disabled key selectors sitting there from the moment the stem loads would look like output before there is any. | retain | — | `Notes lane / N22b` |
| Notes lane | `N23` | `NOTE-026` | Hiding a lane also **mutes** it, independently of the other lane. A pane you cannot see must not still be sounding, because nothing on screen would stop it. | retain | — | `Notes lane / N23` |
| Notes lane | `N24` | `NOTE-027` | Showing a lane again does **not** unmute it. The mute is a separate decision, per lane. | retain | — | `Notes lane / N24` |
| Notes lane | `N25` | `NOTE-028` | Seeking anywhere brings the zoomed window with it, but only when the playhead has left the window — clicking *inside* the zoom pane does not yank the view sideways. | retain | — | `Notes lane / N25` |
| Notes lane | `N26` | `NOTE-029` | The zoomed pane resizes by its own grip, independently of the lane, and the height persists. | retain | — | `Notes lane / N26` |
| Notes lane | `N27` | `NOTE-030` | The zoom width is driven by the `−`/`+` buttons as well as the wheel, and persists. | retain | — | `Notes lane / N27` |
| Notes lane | `N28` | `NOTE-031` | A click in the zoomed pane seeks; a drag pans without seeking. | retain | — | `Notes lane / N28` |
| Notes lane | `N29` | `NOTE-032` | **Whole-phrase detection** under Advanced switches interpreters without re-analysing. | retain | — | `Notes lane / N29` |
| Notes lane | `N30` | `NOTE-033` | It is **on** by default as of v1.14.0, so `hmm-v1` is the shipped interpreter. | retain | — | `Notes lane / N30` |
| Notes lane | `N31` | `NOTE-034` | The **Shortest note** slider stays meaningful in both modes — a duration floor for `threshold-v1`, an onset cost for `hmm-v1`. | retain | — | `Notes lane / N31` |
| Notes lane | `N32` | `NOTE-035` | An unknown interpreter name degrades to `threshold-v1` rather than failing. | retain | — | `Notes lane / N32` |
| Notes lane | `N33` | `NOTE-036` | **Fix octave outliers** never changes the note count. It corrects pitches and marks what it will not correct; it adds and removes nothing. | retain | — | `Notes lane / N33` |
| Notes lane | `N34` | `NOTE-037` | It is **off** by default. | retain | — | `Notes lane / N34` |
| Notes lane | `N35` | `NOTE-038` | A folded note draws **blue**, a doubtful one **gray** — but only while the note is inside the lane's vertical range. **Out-of-band beats provenance:** a note you cannot see at all is the more urgent fact, so it stays orange at the edge whatever its `fix`. | retain | — | `Notes lane / N35` |
| Notes lane | `N36` | `NOTE-039` | A **doubtful note makes no sound** but stays visible. | retain | — | `Notes lane / N36` |
| Notes lane | `N37` | `NOTE-040` | The silence in N36 is **not negligible**: at the shipped `confidentWithin` of 1.5, doubtful notes are **7.5–8.1% of note time**, roughly one note in ten on the worst track. That is the deliberate cost of never claiming a pitch we cannot justify. | retain | — | `Notes lane / N37` |
| Notes lane | `N38` | `NOTE-041` | Folding **never changes pitch class**, so the detected key is unaffected. | retain | — | `Notes lane / N38` |
| Notes lane | `N39` | `NOTE-042` | Both colours and the silence are **reversible**. Unticking restores the previous note list exactly. | retain | — | `Notes lane / N39` |
| Notes lane | `N40` | `NOTE-043` | **Fold tolerance** adjusts `confidentWithin` live, and the readout beside the checkbox updates as it moves. | retain | — | `Notes lane / N40` |
| Notes lane | `N41` | `NOTE-044` | The tolerance slider is **inert while folding is off** — disabled and dimmed, not silently ignored — and the readout is hidden. | retain | — | `Notes lane / N41` |
| Notes lane | `N42` | `NOTE-045` | The slider runs to **8**, so the whole range can be explored — but the readout turns **orange** from 2.5 up, where corrections start being trusted that cannot be justified. | retain | — | `Notes lane / N42` |
| Notes lane | `N43` | `NOTE-046` | The readout's two numbers carry the **lane's own colours** — folded blue, muted gray — so the count and the picture agree without a legend. | retain | — | `Notes lane / N43` |
| Notes lane | `N44` | `NOTE-047` | **簡譜** replaces note names with scale degrees and is **off** by default. | retain | — | `Notes lane / N44` |
| Notes lane | `N45` | `NOTE-048` | The key selectors are set from `detectKey()` on the notes, and go **inert** while 簡譜 is off. | retain | — | `Notes lane / N45` |
| Notes lane | `N46` | `NOTE-049` | The mode selector changes what the **numbers mean**, not just which note is 1. | retain | — | `Notes lane / N46` |
| Notes lane | `N47` | `NOTE-050` | **⇄** swaps the current key for its relative, and swapping twice returns. | retain | — | `Notes lane / N47` |
| Notes lane | `N48` | `NOTE-051` | Touching a key control **stops** the detected key from being re-adopted. | retain | — | `Notes lane / N48` |
| Notes lane | `N49` | `NOTE-052` | The octave is shown **only on the axis**, as 簡譜 dots — never on the note blocks. | retain | — | `Notes lane / N49` |
| Notes lane | `N50` | `NOTE-053` | When the lane is too tight for every semitone, the axis labels the **tonic**, not C. | retain | — | `Notes lane / N50` |
| Notes lane | `N51` | `NOTE-054` | With octave folding on, degrees follow the **corrected** pitch, so the 簡譜 reading changes when the fold controls do. This is correct, not a bug. | retain | — | `Notes lane / N51` |
| Notes lane | `N52` | `NOTE-055` | Loading a **new song** hands each channel's key back to automatic detection, independently, even after an override. The 簡譜 checkbox itself survives the load, per channel. | retain | — | `Notes lane / N52` |
| Notes lane | `N53` | `NOTE-056` | The **zoom pane's** axis matches the lane's: degrees and octave dots, not note names. | retain | — | `Notes lane / N53` |
| Notes lane | `N54` | `NOTE-057` | The **bright gridline follows the tonic**, so the highlighted rule and the highlighted label sit on the same row. | retain | — | `Notes lane / N54` |
| Notes lane | `N55` | `NOTE-058` | A disabled **⇄** looks disabled. | retain | — | `Notes lane / N55` |
| Notes lane | `N56` | `NOTE-059` | The zoomed pane's header carries a **lane selector** on its own row, below the title/seconds row: one labelled chip per stem actually loaded, plus **one "`<lane>` notes" chip per note-capable stem that has a lane** (up to two — vocals and bass), plus the one global Edit-notes toggle. A colour dot alone doesn't say which stem it is, so every plain-waveform chip also carries the stem's own name, and each Notes chip names which channel it selects. Nothing is selected before either channel has notes; the plain-waveform selector's own default is unchanged (`vocals`). | retain | — | `Notes lane / N56` |
| Notes lane | `N56a` | `NOTE-060` | Exactly one Notes chip can be **selected** at a time — clicking one clears the other (`zoomNotesStem` holds a single value, never a set); clicking the already-selected chip clears the selection entirely, removing the pitch grid/contour/note blocks from the pane while the pane itself stays open (N59). Selection is claimed automatically by whichever channel **finishes analysis first**; analysing the other channel afterward does not steal it away. | retain | — | `Notes lane / N56a` |
| Notes lane | `N56b` | `NOTE-061` | Each Notes chip's **mute** glyph is independent of the other's, and independent of which chip is currently *selected* — muting bass's synthesised notes does not affect whether the pane is currently *showing* bass's pitch overlay, or vice versa. | retain | — | `Notes lane / N56b` |
| Notes lane | `N56c` | `NOTE-062` | The **Edit notes** toggle is one global control beside the two Notes chips, not duplicated per panel. It is disabled until the currently-selected chip's channel has notes, and switching chips while it is on turns editing off rather than leaving it silently pointed at an unselected (or note-less) channel. | retain | — | `Notes lane / N56c` |
| Notes lane | `N56d` | `NOTE-063` | Switching which chip is selected **never loses the other channel's edits** — they are held independently in each channel's own `editGroups`, never overwritten. | retain | — | `Notes lane / N56d` |
| Notes lane | `N57` | `NOTE-064` | Clicking a stem's chip adds or removes **its waveform** from the pane; any combination can be selected at once. | retain | — | `Notes lane / N57` |
| Notes lane | `N58` | `NOTE-065` | While **Notes** is selected, every selected stem's waveform renders **gray** behind the note blocks, whatever its own stem colour — the notes are the thing this pane exists to show. With Notes off, each selected waveform renders in its **own stem colour**, one or many at once. | retain | — | `Notes lane / N58` |
| Notes lane | `N59` | `NOTE-066` | With **no** Notes chip selected, the pitch grid, contour line and note blocks disappear entirely — there is nothing pitched to plot them against, and drawing an axis with nothing on it would be worse than no axis. The pane itself stays open, still showing whatever plain waveforms are checked and the time ruler. **The beat/bar grid is the exception**: it draws whenever any channel that has notes has `tempo.on` true — read from `anyRibbon()`, not the selection — because it's a tempo reference for whatever waveform(s) are on screen, not something the pitch view owns or something that vanishes just because no channel is selected. | retain | — | `Notes lane / N59` |
| Notes lane | `N60` | `NOTE-067` | A speaker glyph beside each stem chip mutes/unmutes that lane exactly like clicking its row in the main lane list, and both stay in sync regardless of which one is clicked. **Each** Notes chip carries the same glyph, wired to its own channel's mute (`ribbonMuted(stem)`) — a separate decision from whether that channel's notes overlay is currently shown at all. | retain | — | `Notes lane / N60` |
| Notes lane | `N61` | `NOTE-068` | The plain-waveform lane selection is **not persisted** — it resets to `vocals` on every page load, unlike the zoom width and pane height. `zoomNotesStem` is likewise never persisted — a fresh page load starts with nothing selected until a channel finishes analysis (N56a). | retain | — | `Notes lane / N61` |
| Notes lane | `N62` | `NOTE-069` | The zoom lane's own label ("局部放大" / "Zoom") sits on the **same row as the seconds readout and zoom buttons**, never truncated — the bug this replaced squeezed all three into a 128px column. The lane selector is a **separate row below**, so it reads as its own control rather than a caption for the title. | retain | — | `Notes lane / N62` |
| Notes lane | `N63` | `NOTE-070` | The per-stem **Notes chip** (`.zoom-notes-chip`, wrapped in its `.zoom-chip`) and the one global **Edit notes** toggle (`.zoom-edit-toggle`) are hidden until detection actually has something for them to control — unlike the zoomed pane and overview lane around them, which are always visible (N18, N64). Each chip is hidden independently: it appears once its own channel is both visible and populated. The Edit toggle appears once **at least one** channel is. | retain | — | `Notes lane / N63` |
| Notes lane | `N64` | `NOTE-071` | A shared **overview** lane sits directly above the zoomed pane (N5) — a full-song, never-windowed waveform combining whichever stems are selected below: every stem toggled on via the zoom pane's plain-waveform chips (`zoomLaneSel`), plus whichever channel's Notes chip is selected (`zoomNotesStem`) if any, each overlaid in its own colour. A stem selected only via its Notes chip still draws as a **plain waveform** here — the overview never draws pitch/notes, regardless of what the zoomed pane below it is showing. It is always visible under the same condition as the zoomed pane (N18), independent of note detection, and click/drag-to-seek like any other lane. | retain | — | `Notes lane / N64` |
| Notes lane | `N65` | `NOTE-072` | The overview lane's canvas is exactly the same width, at the same page x-position, as every other lane's — it shares the plain `.lane` grid (label / wave / vol columns) rather than a custom layout, so the playhead lands at the same x on every row including this one. | retain | — | `Notes lane / N65` |
| Notes lane | `N66` | `NOTE-073` | The overview lane's volume slider is not a per-stem control — it's a combination of several stems, so there's nothing single to mute or fade — it mirrors the **master** volume (`#master-vol`) instead, live and in both directions. | retain | — | `Notes lane / N66` |
| Tempo grid | `T1` | `TEMPO-001` | `#notes-tempo` itself is hidden — not merely disabled — until a real detection has actually produced a confident result (`tempo.confidence > 0`), same principle as the notes panels: default-120 BPM controls sitting there before any drums audio has been examined would be output before there is any. Resets to hidden on every song load (`resetTempo()` zeroes `confidence`). | retain | — | `Tempo grid / T1` |
| Tempo grid | `T2` | `TEMPO-002` | Running **Find notes** with a drums stem present detects tempo in the same pass — no separate button press needed — and that is also the moment `#notes-tempo` first becomes visible (T1), already populated rather than appearing empty first. | retain | — | `Tempo grid / T2` |
| Tempo grid | `T3` | `TEMPO-003` | The grid is **on** by default once detected. | retain | — | `Tempo grid / T3` |
| Tempo grid | `T4` | `TEMPO-004` | Toggling **Show tempo grid** off removes the grid from both panes without touching the notes. | retain | — | `Tempo grid / T4` |
| Tempo grid | `T5` | `TEMPO-005` | Editing BPM, phase, or beats-per-bar updates the grid **live, with no re-analysis**. | retain | — | `Tempo grid / T5` |
| Tempo grid | `T6` | `TEMPO-006` | **×½ / ×2** halve/double the BPM field and the grid re-spaces to match. | retain | — | `Tempo grid / T6` |
| Tempo grid | `T7` | `TEMPO-007` | The grid **never changes the note list**. This is the design's central non-goal. | retain | — | `Tempo grid / T7` |
| Tempo grid | `T8` | `TEMPO-008` | **Select BPM range** arms a drag surface across the **whole** drums lane, distinct from the note-editing range-select's bottom-strip-only band. Reachable only after the panel's first appearance (T1) — a range can no longer be pre-selected before ever running detection once, only narrowed afterward and re-detected (T12). | retain | — | `Tempo grid / T8` |
| Tempo grid | `T9` | `TEMPO-009` | Dragging on the armed drums lane commits a selection and updates the caption underneath it; the caption reads "whole song" when nothing is selected. | retain | — | `Tempo grid / T9` |
| Tempo grid | `T10` | `TEMPO-010` | **Clear** reverts to the whole song, both in the caption and in what a subsequent Re-detect analyses. | retain | — | `Tempo grid / T10` |
| Tempo grid | `T11` | `TEMPO-011` | Selecting a range does **not** itself trigger detection. | retain | — | `Tempo grid / T11` |
| Tempo grid | `T12` | `TEMPO-012` | **Re-detect tempo** re-runs detection using the current range **without re-running vocals analysis**. | retain | — | `Tempo grid / T12` |
| Tempo grid | `T13` | `TEMPO-013` | With the drums-lane range-select off, the lane's normal click-to-seek behaviour is unaffected. | retain | — | `Tempo grid / T13` |
| Tempo grid | `T14` | `TEMPO-014` | Export/import round-trips `tempo` and `tempoRange` exactly — both now hoisted to the top of the shared edits file, not duplicated per stem. | retain | — | `Tempo grid / T14` |
| Tempo grid | `T15` | `TEMPO-015` | ⚠ A song with a non-metrical intro (e.g. spoken narration) detects a materially different — and by ear, better — BPM once the range excludes the intro. | retain | — | `Tempo grid / T15` |
| Tempo grid | `T16` | `TEMPO-016` | The grid also draws — very faintly, bars only — across each **stem lane's own waveform**, not just the notes lane; the overview wave at the top never gets it. | retain | — | `Tempo grid / T16` |
| Tempo grid | `T17` | `TEMPO-017` | Two toggle buttons (**½** / **¼**) beside the zoomed pane's zoom in/out controls show dotted sub-beat lines **in the zoomed pane only** — off by default. **¼** draws all three quarter-beat points per beat (which includes the half-beat point, drawn fainter); **½** draws just the half-beat point, in a slightly stronger dash, layered on top. Neither appears on the whole-song ribbon or the per-lane grid — those stay bars/beats only. **¼ implies ½**: clicking **¼** on also switches **½** on (so the half-beat point it draws is never contradicted by an off ½ button); clicking **½** off also switches **¼** off. **½** alone is a valid state; **¼** alone is not reachable through the UI. | retain | — | `Tempo grid / T17` |
| Tempo grid | `T18` | `TEMPO-018` | The ½/¼ toggle buttons persist for the session (not across a page reload) — same as the zoomed pane's lane selection — and follow the beat grid's own on/off and BPM/phase live-editing, since they share `beatTimes`'s math via `subdivisionTimes`. | retain | — | `Tempo grid / T18` |
| Note editing | `E1` | `EDIT-001` | The one global **Edit notes** toggle (`#notes-edit`, beside the two Notes chips, not inside either panel) is disabled until the currently-selected channel has completed a note detection run, and resets (disabled, unticked) whenever a channel that was being edited resets on a new song. | retain | — | `Note editing / E1` |
| Note editing | `E2` | `EDIT-002` | Ticking it turns on note selection in the zoomed pane; clicking a note outlines it in white. Clicking empty space still seeks, exactly as before edit mode existed. | retain | — | `Note editing / E2` |
| Note editing | `E3` | `EDIT-003` | The toolbar is hidden while edit mode is off, and every button but **+ Add note** is disabled until a note is selected. | retain | — | `Note editing / E3` |
| Note editing | `E4` | `EDIT-004` | **↑ 8ve** / **↓ 8ve** move the selected note a full octave and recolour it purple (`NOTE_FILL.manual`), without changing `#notes-count-vocals` (or `-bass`). | retain | — | `Note editing / E4` |
| Note editing | `E5` | `EDIT-005` | **♯** / **♭** move the selected note one semitone, same recolouring rule. | retain | — | `Note editing / E5` |
| Note editing | `E6` | `EDIT-006` | **◀t** / **▶t** move the selected note in time without changing its duration, and recolour it purple (`NOTE_FILL.manual`) — same rule as E4/E5, since a time-adjust is as much a human override as a pitch change. | retain | — | `Note editing / E6` |
| Note editing | `E7` | `EDIT-007` | Dragging the BODY of a note moves it in time — one gesture handles both selecting and moving, even on an unselected note's very first click, with no separate select-then-drag step required. Dragging within ~8px of an EDGE resizes just that edge, but only once the note is already selected (its edge tabs need to be visible first — see E2/E24). Neither ever shrinks a note below a 20ms floor. Either drag recolours the note purple, same as E6. | retain | — | `Note editing / E7` |
| Note editing | `E8` | `EDIT-008` | **✕** deletes the selected note, `#notes-count-vocals` (or `-bass`) drops by one, the toolbar disables. | retain | — | `Note editing / E8` |
| Note editing | `E9` | `EDIT-009` | **✂** splits the selected note at the current playhead position, composing a shrink plus a new note — unless the cut is within 5ms of either edge, in which case it only shrinks (no new note). Both the shrunk original and the new tail note end up purple, since a time-adjust now recolours too. | retain | — | `Note editing / E9` |
| Note editing | `E10` | `EDIT-010` | A split note can be split again — the tail piece is an ordinary note, not a special case. | retain | — | `Note editing / E10` |
| Note editing | `E11` | `EDIT-011` | **+ Add note** arms placement (its label and colour change); the next drag in the pane places a new purple note at the dragged span and pitch row, then disarms. Clicking it again without dragging cancels. | retain | — | `Note editing / E11` |
| Note editing | `E12` | `EDIT-012` | The bottom ~16px band is visibly marked at rest (a faint tinted strip with a top border) while edit mode is on, with a caption below the pane naming what it's for — not just discoverable by trial and error. Dragging it selects a time range (a brighter amber highlight, distinct from the resting strip) and enables **Delete range**; clicking it removes every note overlapping that range. | retain | — | `Note editing / E12` |
| Note editing | `E13` | `EDIT-013` | The edit list shows one row per action (a split is ONE row, not two), each removable with its own ✕; removing one re-derives without it. | retain | — | `Note editing / E13` |
| Note editing | `E26` | `EDIT-014` | Each channel's own edit list (`#notes-edits-vocals` / `#notes-edits-bass`) is a collapsible `<details>`, collapsed by default even with edits present, so a long editing session doesn't keep pushing the zoomed pane down the page. Its summary names the count. Making a new edit or removing one never re-collapses a panel the user opened. When open, the row of controls floats over the page (`position: absolute`) rather than displacing anything below it, and a pointerdown anywhere outside the open `<details>` closes it again — clicking the summary itself, or a row's own ✕/↺ button, does not count as "outside". | retain | — | `Note editing / E26` |
| Note editing | `E27` | `EDIT-015` | Each of Start, End, Pitch shows a visible label above it. Selecting a note populates Start (`m:ss.mmm`), End (`m:ss.mmm`) and Pitch's three dropdowns (letter, accidental, octave — e.g. `D`/*(blank)*/`4` for D4) with its current values, and enables everything plus Apply — same as the toolbar buttons (E3). Selecting a different note updates every field/dropdown to the new note's values. Everything stays populated and enabled until the note itself is gone (deleted, or Edit notes toggled off) — clicking empty space seeks the playhead (E2) without deselecting, so nothing blanks either. | retain | — | `Note editing / E27` |
| Note editing | `E28` | `EDIT-016` | Changing Start and/or End and pressing Enter (in either) or clicking Apply dispatches exactly one `timeAdjust` if the resulting values differ from the note's current ones, and nothing if they don't. Changing any of the three Pitch dropdowns commits immediately — no Enter or Apply needed — dispatching one `pitchNudge` if the resulting note differs from the current pitch, same as the toolbar's ♯/♭ buttons, and nothing if it resolves to the same pitch (e.g. re-picking an equivalent spelling). | retain | — | `Note editing / E28` |
| Note editing | `E29` | `EDIT-017` | An unparseable value in any field (bad time format, unrecognised note name), or a Start/End pair that would violate the 20ms floor or go negative, reverts that field to the note's current value and dispatches nothing for it — without blocking a valid edit in the other field(s). | retain | — | `Note editing / E29` |
| Note editing | `E30` | `EDIT-018` | Pressing Escape while any field has focus reverts all three fields to the note's current values without committing anything. | retain | — | `Note editing / E30` |
| Note editing | `E31` | `EDIT-019` | While a field has focus, no redraw or selection-sync tick overwrites what's being typed — including during playback, when `draw()` runs every animation frame. | retain | — | `Note editing / E31` |
| Note editing | `E32` | `EDIT-020` | Changing both Start and End together and committing produces exactly one `timeAdjust` edit-list row, not two — mirroring how a two-edge drag is already one edit. | retain | — | `Note editing / E32` |
| Note editing | `E33` | `EDIT-021` | Picking a flat accidental resolves to the same pitch as its sharp spelling, including the two letters whose flat crosses an octave boundary: `Cb` is the same pitch as the B *below* it, and `Fb` is the same pitch as E in the same octave. | retain | — | `Note editing / E33` |
| Note editing | `E14` | `EDIT-022` | An edit whose target no longer exists shows a warning glyph in the list rather than silently vanishing. | retain | — | `Note editing / E14` |
| Note editing | `E15` | `EDIT-023` | Cmd/Ctrl+Z undoes the most recent edit, list-order. The same button (↺) does the same thing. | retain | — | `Note editing / E15` |
| Note editing | `E16` | `EDIT-024` | With a note selected, ↑/↓ nudge pitch, Shift+↑/↓ shift octave, Delete/Backspace deletes, `G` snaps it to the grid (E38). ←/→ do NOT nudge a selected note's time — since the ergonomics batch (v1.16.1) they always seek the transport instead (see E20); time is adjusted via the toolbar's ◀t/▶t buttons or by dragging. | retain | — | `Note editing / E16` |
| Note editing | `E17` | `EDIT-025` | **Export edits** — one shared button in the zoomed pane, beside the Edit-notes toggle, covering every analysed channel — downloads a single JSON file: `version: 3`, one shared `tempo`/`tempoRange`, and a `stems` object keyed by stem id, each entry holding `interpreter`, `params`, `clip`, `jianpu`, and `edits` (one `{edits, label?, timeLabel?}` object per list entry — `edits` has two elements for a split — so a batch's row label, e.g. "Snap to grid", survives the round trip instead of falling back to the generic multi-edit "Split" label). **Import edits**, the matching shared button, restores every control and the edit list for each stem the file and the currently loaded song both have, then re-derives each one's note list. See `lib/notes-edits.js` for the format and `docs/superpowers/specs/2026-08-31-note-editing-design.md`/the notes-shared-edit-io work for how it replaced the old per-panel pair. | retain | — | `Note editing / E17` |
| Note editing | `E42` | `EDIT-026` | Every time value that reaches an edit (`at`/`dStart`/`dEnd`/`start`/`end`/`from`/`to`), a note's own `start`/`end`, or `tempoRange`'s `from`/`to` is rounded to millisecond precision (`lib/time.js`'s `roundSeconds`) before it is stored, dispatched, or exported — never the raw drag/arithmetic float (e.g. `207.45864999999998`). One shared function, called from `dispatchEdit` (app.js), `interpret()`/`applyEdits()` (lib/pitch.js), and `snapNoteEdges` (lib/ribbon.js), so no future call site can drift from it. | retain | — | `Note editing / E42` |
| Note editing | `E34` | `EDIT-027` | **Export list** (`#notes-list-export-vocals` / `#notes-list-export-bass`) downloads a self-contained HTML file (inline `<style>`, no external assets) of that channel's current 簡譜 reading laid out on the tempo grid — a separate, human-readable export from **Export edits**' JSON round-trip. Disabled **only** while there are zero notes (via `syncJianpuControls()`) — unlike the key selectors, it does NOT need 簡譜 itself to be on: `jianpu.tonic`/`jianpu.mode` always hold a real value, either the auto-detected key (once notes exist) or the C-major default before any override, so there's always a key to export against. The "Bars per line" field (`#notes-list-bars-vocals` / `#notes-list-bars-bass`, default 4) sets how many bars each line wraps at. Bar boundaries come from `lib/ribbon.js`'s `beatTimes()` against the shared tempo grid (the same grid the lane/zoomed-pane overlay draws from), padded with a leading pickup bar if playing notes start before the first detected bar and a trailing boundary at the song's end. Directly under the title, a `♩ = <bpm> <beatsPerBar>/4` line names the same shared tempo (`tempo.bpmValue` to one decimal, `tempo.beatsPerBar`) the "Show tempo grid" panel already reads from — the note value is always a quarter, matching `noteRhythm`'s fixed 4-units-per-beat grid. Each line is bracketed by a `\ | retain | — | `Note editing / E34` |
| Note editing | `E44` | `EDIT-028` | **Export list** prints a chord guess above each bar from a chromagram over whichever guitar/piano/bass stems are loaded (`notes.js` mixes the stems; `lib/chords.js` drives `lib/chroma.js`). Each bar splits at its time MIDPOINT and each half is matched to `{maj, min, 7, min7, sus2, sus4}` templates by Pearson correlation. The vocal channel's current key — automatic detection or a user-selected override — modestly prefers its diatonic triads, then a lightweight sequence decoder chooses a coherent path through the local candidates; strong local chromatic evidence still wins. When bass notes have been analysed and the longest-overlapping bass pitch class differs from the matched root, the label uses slash notation such as `E/G#`; otherwise it renders the bare chord label. The first label is top-left and a differing second label is centered; `second` is `null` for silence or a repeat. With none of guitar/piano/bass loaded, no chord row is rendered. | retain | — | `Note editing / E44` |
| Note editing | `E18` | `EDIT-029` | Loading a new song clears each channel's own edit list independently, exactly as its parameters and 簡譜 key already reset. | retain | — | `Note editing / E18` |
| Note editing | `E19` | `EDIT-030` | With two notes overlapping in time, a click resolves to the note whose pitch matches the click's vertical position — not an arbitrary array-order match. Only when two overlapping notes share the exact same pitch too (an exact duplicate) does it fall back to whichever is drawn on top (the later array entry). | retain | — | `Note editing / E19` |
| Note editing | `E20` | `EDIT-031` | In the zoomed pane, plain scroll seeks the playhead; Shift+scroll zooms. Arrow Left/Right always seek — proportional to the current zoom span (`zoomSeconds × 0.15`); Shift is a fixed 1ms step for placing a cut inside a word — whether or not a note is selected. | retain | — | `Note editing / E20` |
| Note editing | `E21` | `EDIT-032` | Range-select-and-delete works identically in the full-song notes lane as in the zoomed pane: a resting-state strip and caption whenever edit mode is on, a brighter highlight while dragging or committed, and the same **Delete range** button. | retain | — | `Note editing / E21` |
| Note editing | `E22` | `EDIT-033` | Clicking (pointerdown+up with no real movement) any note — selected or not — seeks the playhead to the exact clicked point, not the note's midpoint, and dispatches no edit; the same click-down also selects it if it wasn't already. A real drag from that same pointerdown instead moves (or, on an already-selected note, resizes near an edge) it, without seeking. | retain | — | `Note editing / E22` |
| Note editing | `E23` | `EDIT-034` | An `add`ed or split-off note sounds through the notes lane's reference tone (with the lane unmuted), even though it sits appended at the end of the note list regardless of its own chronological position. | retain | — | `Note editing / E23` |
| Note editing | `E24` | `EDIT-035` | The selection outline draws on exactly the selected note — never on a different note that happens to share the same time point at a different pitch. | retain | — | `Note editing / E24` |
| Note editing | `E25` | `EDIT-036` | A toolbar button or keyboard shortcut (octave, semitone, time nudge, delete, split) always acts on the exact note carrying the outline — never a different note that happens to share the outlined note's anchor time. | retain | — | `Note editing / E25` |
| Note editing | `E35` | `EDIT-037` | **Export edits** downloads one shared filename, `sans_bass_<song>_notes_edits_<YYYY_MM_DD_HH_MM>.json` (`<song>` is `song` with no mix track loaded), timestamped at export time so a repeat export never silently overwrites the last download — not per-stem, and there is no top-level `"stem"` field any more; each stem's own data lives at `stems.<id>` instead. **Export list** is unaffected by this change in payload shape and stays per-channel, with the same `sans_bass_..._<timestamp>` filename convention and its own suffix: `sans_bass_<song>_vocals_notes_<timestamp>.html` / `sans_bass_<song>_bass_notes_<timestamp>.html` — `<title>`/`<h1>` still name which channel it came from, with the stem word always in English (`Vocals`/`Bass`) regardless of UI language, matching the existing English-only major/minor convention. | retain | — | `Note editing / E35` |
| Note editing | `E36` | `EDIT-038` | A well-formed edits file naming a stem the current song doesn't have loaded (e.g. a saved `guitar` entry, before guitar note detection exists) **skips just that stem and warns**, naming it — every stem the file and the song both have still imports normally. There is no back-compat path for older format versions — the old per-stem single-stem files (`{version:1, stem, edits, ...}`, from before the shared button replaced the per-panel pair), or a `version: 2` file (from before a batch's `edits` entries carried `label`/`timeLabel`) — one of those is just rejected as not a recognized edits file, same as any other malformed JSON. | retain | — | `Note editing / E36` |
| Note editing | `E37` | `EDIT-039` | In the zoomed pane, dragging a note's edge (resize) or body (move) snaps to the tempo grid whenever it's on — to the finest enabled resolution (beat, or half/quarter if those toggles are lit). A move snaps its start and carries the same offset into its end, so duration is preserved exactly; a resize snaps only the edge being dragged. With the grid off, dragging is unsnapped exactly as before. | retain | — | `Note editing / E37` |
| Note editing | `E38` | `EDIT-040` | **⊞ Snap** (toolbar, and the `G` key with a note selected) snaps just the selected note's start and end to the current grid resolution (E37's ½/¼ toggles) as one ordinary `timeAdjust` edit — indistinguishable in the edit list from a manual resize that happened to land on grid values. Disabled without a selection or with the tempo grid off; a no-op (no new row) if the note is already exactly on-grid. | retain | — | `Note editing / E38` |
| Note editing | `E39` | `EDIT-041` | **Snap range** snaps every note overlapping the selected range (E12/E21) to the grid in ONE batch: however many notes move, they land as a single edit-list row, and one Undo/Cmd+Z reverts all of them together — never one row per note. Disabled without a range selected or with the tempo grid off. Two notes that don't already overlap can never end up overlapping purely from this (`lib/ribbon.js`'s `snapNotesToGrid`); a note short enough for both edges to land on the same grid point gets floored to a minimum 20ms width instead of collapsing to nothing. | retain | — | `Note editing / E39` |
| Note editing | `E40` | `EDIT-042` | **Whole song** sets the range selection to the entire song in one click — a shortcut into Snap range (E39) and Delete range (E12/E21) alike, instead of dragging a range band across the full width by hand. | retain | — | `Note editing / E40` |
| Note editing | `E41` | `EDIT-043` | The range-select band (E12) is also available on the **Overview** lane (the combined-stems strip above the zoomed pane), not just the zoomed pane and each stem's own lane — sharing the same selection, so a drag on any of the three surfaces updates what the other two show. Gated on an edit target being selected at all (E1), since Overview has no single stem identity to match against. | retain | — | `Note editing / E41` |
| Note editing | `E42` | `EDIT-044` | With a range selected (E12/E21) — which, per E43, means no note is selected — `Delete`/`Backspace` and `G` mirror the Delete range/Snap range buttons (E12/E39): same edits, same batching. | retain | — | `Note editing / E42` |
| Note editing | `E43` | `EDIT-045` | Selecting a note and having a range selected are mutually exclusive. Clicking a note (or placing a new one via + Add note) clears any active range selection; creating or committing a range selection — dragging a range band on any surface (E12/E21/E41) or clicking Whole song (E40) — clears any selected note. At most one of the two is ever active. | retain | — | `Note editing / E43` |
| Saving stems | `Z1` | `EXPORT-001` | ⚠ Saving writes `<song>/{vocals,guitar,bass,drums,piano,other}.wav` in one ZIP. | retain | — | `Saving stems / Z1` |
| Saving stems | `Z2` | `EXPORT-002` | ⚠ Non-ASCII song titles survive the round trip (general purpose bit 11). | retain | — | `Saving stems / Z2` |
| Saving stems | `Z3` | `EXPORT-003` | ⚠ Stems are encoded one at a time so the WAV bytes are never all live at once. | retain | — | `Saving stems / Z3` |
| Saving stems | `Z4` | `EXPORT-004` | Save re-enables itself after a failure. | retain | — | `Saving stems / Z4` |
| Language | `N1` | `LANG-001` | Default is zh-TW. English only when the system language is not Traditional Chinese. | retain | — | `Language / N1` |
| Language | `N2` | `LANG-002` | A stored choice beats detection; detection never reads storage. | retain | — | `Language / N2` |
| Language | `N3` | `LANG-003` | The first visit does **not** persist a locale. Only clicking the switcher does. | retain | — | `Language / N3` |
| Language | `N4` | `LANG-004` | ⚠ Switching language never disturbs audio. No reload, no re-decode, no re-render of waveforms. | retain | — | `Language / N4` |
| Language | `N5` | `LANG-005` | Everything already on screen re-renders: mode dropdown (selection preserved), lane names, loop badge, all-toggle label, status line, separation status. | retain | — | `Language / N5` |
| Language | `N6` | `LANG-006` | The mode dropdown's option **value** is a stable key (`mix`, a stem id, `lane:<i>`, `custom`), never the label. | retain | — | `Language / N6` |
| Language | `N7` | `LANG-007` | ⚠ Lane labels translate; **stem ids and filenames never do**. | retain | — | `Language / N7` |
| Language | `N8` | `LANG-008` | Zip errors are translated by their stable `code`; an unknown code falls back to the original English message. | retain | — | `Language / N8` |
| Language | `N8a` | `LANG-009` | Both halves of the hint line translate, markup included. | retain | — | `Language / N8a` |
| Language | `N9` | `LANG-010` | The switcher is visible and both halves are reachable. | retain | — | `Language / N9` |
| Language | `N10` | `LANG-011` | `<html lang>` and the tab title follow the locale. | retain | — | `Language / N10` |
| Language | `N11` | `LANG-012` | A blocked or throwing `localStorage` degrades to detection instead of killing the app. | retain | — | `Language / N11` |
| Language | `N12` | `LANG-013` | ⚠ A status param that is itself translated is re-resolved on switch, never stored rendered. | retain | — | `Language / N12` |
| Visibility | `V1` | `BOOT-001` | Anything with the `hidden` attribute is actually invisible. | retain | — | `Visibility / V1` |
| Visibility | `V2` | `BOOT-002` | Every hidden-toggle in `app.js` and `separate.js` depends on V1. | retain | — | `Visibility / V2` |
| Loading the page itself | `G1` | `BOOT-003` | A missing element does not stop `app.js`. Every top-level listener goes through `on()`, which warns and continues. | retain | — | `Loading the page itself / G1` |
| Loading the page itself | `G2` | `BOOT-004` | An uncaught error puts a message on screen naming the force-reload, instead of leaving a page that looks fine and does nothing. | retain | — | `Loading the page itself / G2` |
| Loading the page itself | `G3` | `BOOT-005` | Every asset Vite's build touches is content-hashed, and a fresh `index.html` never references a stale hashed file. | retain | — | `Loading the page itself / G3` |
| Loading the page itself | `G4` | `BOOT-006` | The page is served over HTTP — GitHub Pages, or `npm run dev` locally. `file://` is no longer supported (dropped in v1.5.0); opening `index.html` from disk is not expected to work. | retain | — | `Loading the page itself / G4` |
| Loading the page itself | `G5` | `BOOT-007` | A dim `<git short SHA>` sits fixed in the bottom-right corner of every build (dev, PR preview, main) — the exact commit that produced it, baked in at build time by `vite.config.js`'s `__COMMIT_SHA__`. Lets a manual/browser-automation check confirm it is testing the deploy it thinks it is, rather than a stale cached page (see the v1.24.0 devlog gotcha). | retain | — | `Loading the page itself / G5` |
| Analytics | `A1` | `ANALYTICS-001` | Analytics never breaks the player. With `window.SansAnalytics` deleted, every control still works. | retain | — | `Analytics / A1` |
| Analytics | `A2` | `ANALYTICS-002` | `play` fires once per page load, from the user gesture only. | retain | — | `Analytics / A2` |
| Analytics | `A3` | `ANALYTICS-003` | Interaction counts emit power-of-two buckets. | retain | — | `Analytics / A3` |
| Analytics | `A4` | `ANALYTICS-004` | No event name carries user content. | retain | — | `Analytics / A4` |
| Analytics | `A5` | `ANALYTICS-005` | Events fired before GoatCounter loads are not lost. | retain | — | `Analytics / A5` |
| Analytics | `A6` | `ANALYTICS-006` | Events do not reach GoatCounter from localhost. | retain | — | `Analytics / A6` |
| Analytics | `A7` | `ANALYTICS-007` | `separate-handheld-blocked` fires **once** per visitor shown the message, never once per poll. | retain | — | `Analytics / A7` |
